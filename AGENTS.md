# AGENTS.md

## Repository Conventions

- JS files are validated with `jsconfig.json` and `checkJs=true`; prefer lightweight JSDoc typedefs and comments that satisfy TypeScript-style checking.
- Existing source style uses concise JSDoc blocks for typedefs and function parameter and return annotations in plain `.js` files.
